/* Ejemplo para ingresar la contraseña tres veces:

let contraseña;
let intentos = 0;

while(contraseña !== "adaitw" && intentos < 3) {
   contraseña = prompt("Ingrese la contraseña");
   intentos++;
}

   if (contraseña === "adaitw") {
       alert("Login correcto");
       
   } else {
       alert("Login incorrecto");
   }

*/



